#include <stdio.h>

int main(){

    char nome[3][50] = {
        "Gabriel", "Henrique", "Adson"
    };

    int numNomes = sizeof(nome) / sizeof(nome[0]);
    
    int i;

    for(i = 0; i<numNomes; i++){
        printf("%s\n", nome[i]);
    }

    return 0;


}
